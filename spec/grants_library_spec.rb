require "spec_helper"

describe GrantsLibrary do
  it "has a version number" do
    expect(GrantsLibrary::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
